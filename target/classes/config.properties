# Browser Configuration
browser=chrome
headless=false

# Application URLs
url=https://www.amazon.in

# Environment Configuration
environment=test

# Timeout Configuration (in seconds)
implicit.wait=20
explicit.wait=30
page.load.timeout=30

# Test Execution Configuration
thread.count=1
parallel.execution=false

# Screenshot Configuration
screenshot.on.failure=true
screenshot.path=output/screenshots/

# Reporting Configuration
report.path=output/reports/
log.path=logs/

# Grid Configuration (for remote execution)
grid.url=http://localhost:4444/wd/hub
remote.browser=chrome

# Application Login Credentials
test.username=testuser
test.password=testpass

# Database Configuration (if needed)
db.url=jdbc:mysql://localhost:3306/testdb
db.username=root
db.password=password

# API Configuration (if needed)
api.base.url=https://api.example.com
api.timeout=30

# Email Configuration for Report Sending
email.enabled=true
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.from=infa.bga1@gmail.com
email.password=zfptliekccnmkrep
email.to=shashikant20100@gmail.com
email.cc=shashikant20100@gmail.com
email.subject.prefix=Automation Test Report
email.max.file.size.mb=10
